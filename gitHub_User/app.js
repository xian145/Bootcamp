document.addEventListener("DOMContentLoaded", ()=> {
    const form = document.querySelector("form") //select the form
    form.addEventListener("submit", (event) => { //add a event whe hear the submit
        
    })
})